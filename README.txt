This simple GUI program demonstrates the Hungarian method.

Requirement:
- Python 3

Usage:
Since the project depends on networkx for graph manipulation and matplotlib for drawing,
you need to run one of the following commands before running the program for the first time.

- pip3 install matplotlib networkx
- python3 -m pip install matplotlib networkx
